export type Gauge = {
  current: number;
  max: number;
};

// stat bonus are computed at runtime
export type Life = {
  current: number;
  max: number;
};

// stat bonus are computed at runtime
export type Drive = {
  current: number;
  max: number;
};

// stat bonus are computed at runtime
// also called Stamina?
export type Spirit = {
  current: number;
  max: number;
};
