// stat bonus are computed at runtime
export class Life {
  current: number;
  max: number;
}

// stat bonus are computed at runtime
export class Drive {
  current: number;
  max: number;
}

// stat bonus are computed at runtime
// also called Stamina?
export class Spirit {
  current: number;
  max: number;
}
