import { Character } from '../contracts/Character';

class LoomeeAngora extends Character {
    protected _name: string = 'Loomee Angora';
    protected _description: string = 'The Keeper of Heart';
}