import { IsDiceNotation } from '../custom/is-dice-notation';

describe('IsDiceNotation', () => {
  describe('create', () => {
    // let faker: SampleClass;

    beforeAll(async () => {
      // faker = await FakerFactory.create<SampleClass>(SampleClass);
    });

    it('placeholder update', () => {
      expect(1).toBe(1);
    });
  });
});
